#task 1. Обмен соседних элементов

lst =[1,2,3,4,5]
result =[]

for i in range(0,len(lst)-1,2):
    result.append(lst[i+1])
    result.append(lst[i])

if len(lst) % 2 != 0:
    result.append(lst[-1])
    
print(result) #[2,1,4,3,5]